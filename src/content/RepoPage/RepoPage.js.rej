diff a/src/content/RepoPage/RepoPage.js b/src/content/RepoPage/RepoPage.js	(rejected hunks)
@@ -56,2 +55,2 @@ const LinkList = ({ url, homepageUrl }) => (
-const getRowItems = (rows) =>
-  rows.map((row) => ({
+const getRowItems = rows =>
+  rows.map(row => ({
@@ -65 +64 @@ const getRowItems = (rows) =>
-}));
+  }));
